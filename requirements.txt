# Core (server / 8xA6000, CUDA 12.x)
torch>=2.4
torchvision>=0.19           # REQUIRED by the Qwen2.5-VL video processor; must match your torch/CUDA
                            # (e.g. pip install torchvision --index-url https://download.pytorch.org/whl/cu121)
transformers>=4.49.0         # Qwen2.5-VL (primary backbone). For the Qwen3-VL bonus row bump to
                             # >=4.57 (or: pip install "git+https://github.com/huggingface/transformers")
qwen-vl-utils>=0.0.8         # required by the PRIMARY backbone (Qwen2.5-VL) input path
accelerate>=0.34
peft>=0.13
decord>=0.6.0                 # video frame reading
faster-whisper>=1.0.3         # ASR (GPU; CPU int8 fallback)
numpy>=1.24
pyyaml>=6.0
Pillow>=10.0
tqdm>=4.66
datasets>=2.20               # scripts/00_convert_benchmarks.py (HF benchmark loading)

# Backbone notes:
#   Qwen2.5-VL-7B (PRIMARY, matches baselines) -> needs qwen-vl-utils (above).
#   LLaVA-Video-7B (generalization row)        -> transformers LLaVA classes.
#   Qwen3-VL-8B (latest-backbone bonus row)    -> transformers>=4.57, no qwen-vl-utils.

# Optional / paper extras
# flash-attn>=2.6              # attn_implementation="flash_attention_2"
# deepspeed>=0.14             # only if trying full-FT instead of LoRA
# matplotlib                   # figures (plot_pareto.py)

# Dev (local, GPU-free logic tests)
pytest>=8.0
