# Core (server / 8xA6000, CUDA 12.x)
torch>=2.4
# Qwen3-VL needs transformers >= 4.57. If 4.57 is not yet on PyPI for your date,
# install from source:  pip install "git+https://github.com/huggingface/transformers"
transformers>=4.57.0
accelerate>=0.34
peft>=0.13
decord>=0.6.0                 # video frame reading
faster-whisper>=1.0.3         # ASR (GPU; CPU int8 fallback)
numpy>=1.24
pyyaml>=6.0
Pillow>=10.0
tqdm>=4.66
datasets>=2.20               # scripts/00_convert_benchmarks.py (HF benchmark loading)

# Backbone-specific:
#   Qwen3-VL (default): no qwen-vl-utils needed (uses processor.apply_chat_template return_dict).
#   Qwen2.5-VL (alt)  : also install  qwen-vl-utils>=0.0.8
# qwen-vl-utils>=0.0.8

# Optional / paper extras
# flash-attn>=2.6              # attn_implementation="flash_attention_2"
# deepspeed>=0.14             # only if trying full-FT instead of LoRA
# matplotlib                   # figures (plot_pareto.py)

# Dev (local, GPU-free logic tests)
pytest>=8.0
